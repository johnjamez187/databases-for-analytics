[project]
name = "databases-for-analytics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "John James", email = "johnthomasjames@netscape.net" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
databases-for-analytics = "databases_for_analytics:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
